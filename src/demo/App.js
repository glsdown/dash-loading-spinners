/* eslint no-magic-numbers: 0 */
import React from 'react';

import {Loader} from '../lib';

const App = () => {
    return (
        <div>
            <Loader id={'testing'} />
        </div>
    );
};

export default App;
